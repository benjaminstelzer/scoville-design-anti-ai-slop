# RC4 final adjudicated Design routing regression v5

Created: 2026-09-02 after v4 execution and a second independent SOL
adjudication.

V4 remains unchanged. V5 narrows two Golds whose prompts did not explicitly
request visual critique or spatial template decisions. It retains the
source-only jurisdiction expectation that exposed Critique over-routing. The
Core/route correction now requires an explicit visual critique concern rather
than the isolated words `audit` or `design`.

V5 is an open regression suite, not fresh unseen evidence. Its copied Test
split remains consumed and is not run or claimed as unseen.
