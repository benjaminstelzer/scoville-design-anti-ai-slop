# Fable review of the expanded application-first Plan

Date: 2026-09-01  
Reviewer request: Claude Fable 5.1, high effort  
Session: `a693bbc9-0188-4730-bbab-a9cf5ba878d1`  
Wrapper-reported model: unavailable  
Cost: USD 2.125097  
Initial verdict: `REVISE BEFORE IMPLEMENTATION`

## Scope

The reviewer read the exact expanded Plan, audit, modular architecture,
style-direction research, maturity ledger, qualification contract, blind
experiment, and ADR-0011/ADR-0012. It was asked only for concrete Blocker,
High, or Medium findings and made no edits.

## Findings and reconciliation

| Severity | Finding | Reconciliation |
| --- | --- | --- |
| Blocker | Module IDs differed among route matrix, package trees, and Motion coverage. | ADR-0008 IDs plus `style-direction` are now canonical everywhere; Motion is explicit; W-009 and validators own drift checks. |
| High | W-007 froze modules before W-008 determined them. | W-007 freezes the maximum stable ID map; W-008 assigns only `admitted`, `retained-floor`, `stub`, or `withheld`. |
| High | W-008 could optimize away sourced functional floors or current-verification routes. | `retained-floor` is source-admitted without SOL evidence and barred from SkillOpt deletion; behavioral deltas use a symmetric sample rule. |
| High | Three-reference/7,000-token budget conflicted with broad task rows. | Core index and module sub-budgets are explicit; every route row is validated; work with more than three experts is phased through a compact Design Dossier. |
| High | No directed `generic-cliché` mutation class existed. | Added with concept-specificity/cliché dimensions, same-brief protection, and observed-SOL-default seeding. |
| High | Editable artifact and render routes were undefined. | Added artifact-class source/render table, missing-renderer boundary, actual-format owner, and at-most-two repair passes. |
| High | Imagery/art direction was only `mapped`. | Targeted research raised it to source-grounded baseline with explicit tool/legal/consistency limits and a 12-case qualification requirement. |
| High | Presentations, documents, reports, forms, and templates had no route. | Added a canonical route, unknown-artifact fallback, W-005 lane, and format/render ownership. |
| Medium | Manifest signal, dependency, and conflict behavior was underspecified. | Version one uses an enumerated signal catalogue and forbids non-empty expert `requires`/`conflicts`; validation fails closed. |
| Medium | Critique expert duplicated domain critique modes. | `critique-and-validation` now owns only method; domain discriminators remain in their experts. |
| Medium | Mode vocabularies conflicted. | Canonical modes are `generate`, `critique`, `repair`, and `style-direction`; exception is a gate and legacy terms are non-manifest aliases. |
| Medium | Repair lacked stop/regression rules. | Maximum two passes; same observation/protected-dimension checks repeat; unresolved regressions are reported. |
| Medium | Design-to-UI record lacked a portable schema/location rule. | Added `scoville.design-direction/v1`; it is inline or user-authorized project state and is never auto-discovered. |
| Medium | W-008 reviewer count was unclear. | One qualified reviewer may admit an internal module without a public claim; W-005 retains three-reviewer cross-person evidence. |
| Medium | Eighteen sealed cases were sparse. | Lanes below five remain smoke-only; a second independent encrypted tranche is being added before freeze; domain claims still need open Validation evidence. |

## Final disposition

The targeted imagery record, second custody tranche, W-009 reconciliation, and
native Plan validation completed. The focused Fable follow-up failed at the
provider session limit; on 2026-09-01 the user explicitly waived that follow-up
and authorized continuation without Fable. The original full Fable review and
its reconciled findings remain process evidence, not product qualification.
