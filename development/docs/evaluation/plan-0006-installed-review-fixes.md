# Independent installed-package review fixes

Date: 2026-09-04. Owner: PLAN-0006 / W-005.

The user requested a plan, implementation and a result review in the same
independent Fable session. The one-off review automation was deleted, not merely
paused. Installation, publication, commits, pushes, tags and releases remain
outside this work. W-004 is paused without weakening its remaining acceptance.

## Finding dispositions

| Fable finding | Disposition and implementation |
| --- | --- |
| Unavailable evidence receipts | Core and metadata identify receipt values as external development IDs, not included files or pass claims. An unavailable outcome stays unverified. No old receipt or result was changed or bundled. |
| Unclear metadata role and index duplication | Core identifies its own runtime authority and the non-routed packaging/evaluation role of `modules.yaml`. The header names the existing generator and drift checks as repository-only tools, not installed dependencies. The generated index still matches exactly. |
| Suspected underestimated token ceilings | Adapted to measured evidence, not accepted as a proven defect. Existing `o200k_base` measurement shows the named files and common loads below their advisory ceilings. Metadata now distinguishes allowances, actual token measurement, separate Core/index accounting and excluded provider overhead. No ceiling, target, count limit or invocation policy was changed. |
| Undefined sibling terms | Canonical optional Skill names have responsibility definitions. The out-of-context Greenfield statement was removed from Design; the actual UI fallback remains unchanged in its owner. The boundary checker now checks that owner and standalone Design operation directly. |
| Weakly sourced generic catalogue | The catalogue now explicitly labels itself as package-authored reviewer heuristics, not a validated AI-authorship or design-quality detector. Its dated selection, legitimate counterexamples and existing local-synthesis provenance remain intact; no external validation was invented. |
| Broad implicit invocation | The description now requires a requested or unresolved visual decision/diagnosis and excludes mechanical settled-design execution, conversion and rendering alone. Automatic selection remains enabled. Actual host selection accuracy is not proved by text or interpreted routing fixtures. |
| Minor wording and absent developer paths | Fixed the source-class conjunction, named Cartography as its own row with a conditional Wayfinding relationship, and explicitly marked developer paths as absent repository-relative audit locators. |

## Focused evidence

- Current package, generated index, 60 interpreted route fixtures, Design/UI
  boundary and Skill Creator validation passed.
- Four focused boundary tests passed, including negative removal checks for
  the UI-owned fallback, standalone Design work and design/implementation roles.
- Fifteen advisory notices concern lower planning targets, not exceeded
  ceilings. They are retained, not suppressed or turned into failures.
- Final authoring Core excluding index: 1913 `o200k_base` tokens against a 2200
  allowance. Generated authoring index: 1173 against 1500. Brand: 2482 against
  2900; Advertising: 2376 against 2900; Media Production: 2331 against 2800.
  These are file measurements, not Fable tokenizer counts or total context cost.
- Exactly five of the 34 runtime files differ. The other 29 and the original
  independent-review package remain byte-identical to their manifests.
- Final source manifest: `7049686B1672BF04EAFF12A69F8A713770E26218730193F961DB64DB1CA15B9D`.
- Final runtime manifest: `B8874386F46DA1319FA75502887152D247318BEE1B50D4F64C9E5D84B9986F9C`.

## Review and limits

Both follow-up reviews completed in the original independent Fable 5.1 / high
session `06c61365-6b5d-4862-8be7-5e0ffc346b62`, using only the new installed-form
packages and that independent review context. No project plans, test results,
remediation rationale or earlier project sessions were supplied. Fable's full
re-review accepted the original dispositions and found two small residues:
one unexplained Scribe name and an unqualified development-check claim.

Only Core and Source Index were then changed: the former says the installed
snapshot does not attest that checks ran; the latter names the wording owner's
authority instead of the unexplained Skill shorthand. After the final package
validation and build, Fable reviewed only those two changed files and found
no remaining actionable problem or regression. It did not repeat the full review.

Actual transcript coverage confirms 34 fully read files in the first follow-up
and both changed files fully read in the final pass. All calls stayed inside
the respective package with Read/Grep/Glob only, no tool errors or permission
denials. Host SHA-256 checks supplement Fable's weaker line-count comparison:
exactly two files changed in the residual pass, and the other 32 are byte-identical.
The original and intermediate installed packages are unchanged. Fable's first
follow-up mentions six targeted files; the actual manifest delta is five.

Evidence is outside the installed package under
`Z:/Projekts/AI/scoville-design-eval-local/fable-installed-review-20260904/`:
`fixes-before/`, `fixes-checks.json`, `fixes-build.json`, `followup-review.md`,
`residual-checks.json`, `final-build.json`, `final-review.md`,
`final-verification.json` and complete `attempt-2/` and `attempt-3/` outputs.
The final runtime directory is
`C:/Users/benja/AppData/Local/Temp/design-independent-review-final-20260904/scoville-design-anti-ai-slop`.

No full holdout, new rendered design comparison, external source verification
or actual host activation was performed in this bounded fix round. Static
checks and reviewer agreement do not complete those separate W-004 lanes.
