# PLAN-0006 focused execution progress

## Current boundary

W-001 through W-003 are implemented. W-004 is in progress, not qualified or
complete. After authorising SOL reviews (ADR-0056), the user separately
authorised matched SOL generation for the remaining pairs (ADR-0057).
The first CLI attempt could not read its inputs because of host policy;
the preserved failure and matched native fallback are documented in
plan-0006-sol-native-protocol.md. No policy is weakened and no completed
C1-C3 pair is repeated.

Source-of-truth protocol: evaluation/plan-0006/protocol-v1.json. Seven families,
with paired solo/partner variants of family 5, were frozen before outputs.
Input hashes and package manifests: plan-0006-focused-freeze.json.
Candidate build: plan-0006-tested-build.json.

- Baseline: 32 files; manifest
  `2D8718FF68019BA7D4AC21943F8A98D7823629B4B07C5BD7AE9EFFC06FE8D91B`.
- Original tested candidate: 34 files; manifest
  `246B03A84383C1E2AB7A4928F597F1F5A32B014734D6AD3A6ED1FBEE65368655`.
- R02-corrected, targeted-retested candidate: 34 files; manifest
  `60B1E02D07EB2BEF4F168652D2D7018274F0814E4D785FDEF5D867A137FFAA60`.
- Final local, evidence-annotated derivative: 34 files; manifest
  `3F38C596AE0ED4107609D874EAE628250802ED9D766616D439832FD985BA380F`.
  Exactly the registry evidence values differ from the targeted-retested
  derivative; every other byte is identical. This is not the old tested hash.
- Workspace:
  `C:/Users/benja/AppData/Local/Temp/design-plan6-focused-v1-20260904`.
- Durable progress archive:
  `Z:/Projekts/AI/scoville-design-eval-local/plan-0006-progress-20260904`.
  This retains prepared cases, packages, sources, proofs and execution traces.
  Isolated authentication/configuration directories were deliberately excluded.
- Initial Fable stratum requested model/effort: `claude-fable-5-1` / high.
  Assistant trace labels confirm that model string; the wrapper's
  optional reported_model is null. No backend identity beyond that trace is
  asserted. Tools are Read/Grep/Glob, safe-mode, no fixed cost/token ceiling.
- Remaining matched native stratum: `gpt-5.6-sol` / high, independently
  confirmed in each captured native turn context. Fresh workers receive no
  conversation history or other outcomes. No outside-workspace reads or extra
  Skills are reported in the first ten case sessions; actual tool-event extracts
  are retained separately from self-reported read lists. Native host permission
  and instruction inheritance is disclosed in the fallback protocol.
  Workspace: `C:/Users/benja/AppData/Local/Temp/design-plan6-sol-native-v1-20260904`.

## Observed work

| Case | Actual execution and evidence | Limit |
| --- | --- | --- |
| C1 small repair | Both generated; host materialised and rendered. Byte-identical outputs change only .copy line-height .7 to 1.4. SOL blind review confirms repair and protected content/layout. Actual Typography reads in both traces. | Only fixed-poster screen proof; no general improvement over baseline. |
| C2 named-style web/brand | Both generated and rendered. Both read Style directly; candidate also reads Generic. Initial blind SOL preference is modestly for baseline. Five actual output defects across both arms receive minimal host corrections as separate v2 files; four new PNGs verified by the same blind reviewer. | Corrected pair still modestly favours baseline. No demonstrated candidate visual superiority. These are review-assisted outputs, not fresh generations. |
| C3 quiet utility | Both generated byte-identical to input. Core only; no unnecessary leaf read. Desktop/mobile rendered; blind SOL review finds appropriate preservation. Author explanations are retained separately. | Finite settled utility, not a general no-regression result. |
| C4 motion/data | Original interrupted Fable stratum retained. Fresh matched SOL sources N01/N02 generated and played at 1000px with 100/400/1000ms captures, video and reduced motion. Additional actual return, rapid retrigger, keyboard Enter and 390px states preserve exact data and capacities. | Candidate first output has an initially visible previous-sample legend. Separate artifact-v2 adds only `.legend [hidden]{display:none}`; actual initial/advanced/reduced proofs rechecked. Blind judgment narrowly favours baseline, medium confidence. Generated-output defect, not a newly demonstrated instruction defect. |
| C5A/C5B mixed-domain | All four SOL outputs rendered at 1000/390px. Protected groups and visible words pass exact checks. Real partner return and design verification completed; candidate partner reads Coordination, baseline reads Media handoff. | Original C5A tie, high confidence; C5B narrow candidate advantage, medium confidence, retained after independent return-loop verification. Flat internal proof only; no physical, route, barcode, safety or production acceptance. |
| C6 unavailable proof | Both arms deliver source-only typography proposals. Exact checks preserve everything outside pack-text, all words and both fixed groups. Both withhold rendered and production claims. Independent source/response review: tie, high confidence. | Intentionally no render supplied or produced. No visual preference can be claimed without a render. |
| C7 read-only critique | Both return supported findings, empty file arrays and unchanged input/package hashes. Candidate reads Generic; both preserve legitimate peer comparison. Independent review: tie, medium-high confidence. | Proposed improvements remain untested; no mutation or new render. |
| C8 Brand extension | Frozen for the actual G09 coverage gap before outputs. Matched N11/N12 execute and render at 1200/390px. Independent review: tie, moderate-high confidence. Source, description and palette-owner defects are retained in v1/v2; corrected v3 sources have actual render and palette-propagation verification. | Tie retained after correction review. All four v2/v3 PNG hashes are identical. Only G09 added; no unnecessary extra UI case. Recognition, physical production and general superiority remain unverified. |

## Targeted R02 correction and retests

The independent full-package/proposal fidelity review identified an actual
implementation omission: five module-local record openings still implied
exhaustive inventories despite Core's relevance rule. Colour, Imagery, Brand,
Mark and Packaging now explicitly select material open fields while retaining
their content, authority, rights, geometry, accessibility and safety floors.
The source disposition was corrected too. This was a source-level ambiguity;
the earlier cases did not demonstrate exhaustive-record behaviour.

Independent follow-up confirms the exact five-reference diff resolves the
finding at instruction level. R02 is not a metadata-only change. Before new
outcomes, `freeze-record-scope-r1.json` froze the new package and unchanged
C5A/C8 task/input material. Fresh SOL/high candidate sessions N13/N14 provide
the only two retests; the baseline is reused, not regenerated.

- N13: concise relevant repair record; all protected bytes/words unchanged;
  actual 1000/390px proof and author verification. Fresh blind review slightly
  prefers the candidate's larger label typography. Original C5A tie remains.
- N14: relevant identity rules and bounded evidence notes, without unrelated
  inventory prerequisites; actual 1200/390px proof and author verification.
  Fresh blind review initially narrowly prefers it over the original baseline,
  then judges a tie after receiving the baseline's already justified v3
  corrections. N14 remains unchanged. Original C8 tie remains separate.

These observations support scoped behaviour in the exercised repairs and
identity task. They do not prove universal record reduction or attribute the
new aesthetic differences to five small instruction clarifications.

## Durable receipts and final local derivative

`plan-0006-case-receipts.json` resolves 20 executed initial case sessions to
actual models/settings, IDs, dates, source/proof/trace hashes and outcomes.
Corrected initial failures remain explicit stages, not overwritten passes.
Twenty-two relevant modules reference observed candidate receipts; all 30
remain draft. Unexercised modules retain empty lists. No baseline receipt is
presented as candidate execution.

The native completion archive contains 958 byte-verified files at
`Z:/Projekts/AI/scoville-design-eval-local/plan-0006-native-completion-20260904`:
native executions, all versions/proofs/reviews, anonymous review inputs,
fidelity inputs and the failed CLI attempt. No authentication/configuration
files are included. Original Fable evidence remains in the earlier archive.

The local installed-form candidate is
`Z:/Projekts/AI/scoville-design-eval-local/plan-0006-local-candidate-20260904/scoville-design-anti-ai-slop`.
This directory is not an installation. `plan-0006-final-local-build.json`
records the source and derivative manifests. The exact annotation check proves
only evidence values changed after the R02 retests; every other runtime byte,
route, mode and draft status is preserved. No visual rerun is warranted for
that metadata-only step.

The final evidence audit found one capture defect, not a Skill or design
failure: the standalone initial `scope-r1/review.md` had selected the subsequent
commentary rather than the completed verdict. The actual initial final answer
at `2026-09-04T08:16:44.658Z` remained intact in its event extract. It is now
recovered into `plan-0006-review-recovery-20260904/scope-r1` under the evaluation
root. The erroneous original capture and archive remain unchanged; the receipt
registry links the corrected capture and records supersession explicitly.
All 20 execution records, comparisons, annotation IDs and runtime bytes are
unchanged. Six focused capture-helper regressions pass; no model or design
case was rerun. The other seven review captures already selected final answers.
The same SOL auditor independently verified the exact recovery and dependent
hashes, then reported no remaining material finding within that bounded audit.
`plan-0006-final-audit.json` records the actual verdict and its limits; it is
not the still-required Fable final review.

`inventory-v1.json` in the execution workspace retains actual read paths,
session IDs, trace hashes, errors and first-generation artifact hashes. Each
attempt keeps stdout, stderr, receipt and raw JSONL; failed-attempt traces were
recovered from their exact session IDs without new model calls.

The C2 baseline answer included a prose preface before one complete JSON object.
The host extracted that complete object without changing its returned source;
the raw answer remains. This was transport parsing, not a model rerun. All
materialised original sources are exact returned content with a final newline.
No other automatic source repair was applied before initial renders.

Native session capture reads only the exact worker transcript selected through
its actual session metadata and verifies agent path, model/effort and frozen
input/package hashes. Captures preserve actual tool calls/results and returned
assistant messages, excluding host instructions and private reasoning. These
are explicitly scoped event extracts, not claimed complete raw transcripts.
Host materialisation adds a final newline where absent; otherwise source is
the exact returned proposal. The original source and correction versions are
retained. The native CLI access failure also remains available despite its
transport exit code zero.

The fresh SOL artifact reviewer receives only the neutral bundle
`C:/Users/benja/AppData/Local/Temp/design-plan6-sol-neutral-8bf7`.
Per-pair assignment is randomised and stored outside that tree. Skill packages,
identity maps, intended criteria, read disclosures and prior findings are not
provided. C6/C7 and the partner handoff include their actual task response with
only provenance/read disclosures removed. Main C4 proofs match each supplied
source, including the candidate's separately preserved correction version.

## Actual host activation diagnostics

H1 first attempt: Claude Code 2.1.257 with isolated CLAUDE_CONFIG_DIR,
read-only tools and --restricted. The target was absent from the real initial
skill_listing (13 built-ins only). The model found the package via Glob and
Read instead. This is **not a valid implicit-activation pass**.

H1-v2 corrected only the harness: a fresh bounded temporary repository,
project-only setting sources, isolated configuration, empty strict MCP and
read-only tools; no safe-mode or restricted mode. The actual initial listing
now contains scoville-design-anti-ai-slop plus host built-ins. The provider then
returned 429 before any model invocation. This demonstrates discovery/listing,
not trigger selection. H2-H5 have not executed.

Official setup references inspected on 2026-09-04:
[credential location](https://code.claude.com/docs/en/authentication),
[CLI isolation flags](https://code.claude.com/docs/en/cli-reference),
[project Skill discovery](https://code.claude.com/docs/en/slash-commands).
Only a temporary login credential copy, never its contents, was used outside
the model workspace. Both exact temporary copies were removed after their
attempts. No personal settings/hooks/Skills or global installation were changed.

The provider error in C4 candidate and H1-v2 is:
`You've hit your session limit · resets 11:10am (Europe/Berlin)`.
This is an external 429, not ADR-0032's advisory resource estimate. No unchanged
retry was started after that error was observed. Already-running generations
were allowed to finish and their outcomes were retained.

## Coverage still required

- Complete host H1-H5 trigger behaviour; listing alone is insufficient.
- Obtain the retained Fable session's final package/evidence judgment when the
  provider is available. ADR-0056 authorises interim SOL review but preserves
  this original acceptance requirement. SOL's clean final source-level verdict
  does not become a Fable approval.

All seven original families, both mixed-domain variants, the justified Brand
extension and two R02 candidate retests now have actual execution/review
evidence. No unrelated rerun or second full holdout is outstanding. W-004
remains in progress because the two required lanes above are unverified.
Comparative disagreement and narrow proof ceilings remain visible, not
averaged into a release-readiness score.

No full holdout, frozen package, old ledger, evaluator gold, installation,
commit, push, tag, release or publication has been changed or initiated.

## Final local deterministic checkpoint

28 unit tests pass, including historical schemas and the new executed-receipt
resolution positives/negatives. Source and installed derivative validation pass
with 13 advisory estimate warnings; no hard budget gate is introduced. The
generated index is current, all 60 interpreted route fixtures pass, and the
Design/UI boundary and Skill Creator checks pass. Actual command outputs are
retained in `plan-0006-final-checks.json`.

Core is 1794 o200k_base tokens; authoring index 1173, combined 2967 versus
3318 before. The installed index is 1159 after the exact 14-token comment
transform. Largest current leaf is 2482; Core/index plus the four-largest
historical measurement sample is 12470 authoring tokens, not an expert cap.
Instruction-file total is 57543 in authoring form. These are file measurements,
not provider-total tokens, cost savings or quality evidence. Required unverified
host/Fable acceptance is not covered by deterministic checks.
