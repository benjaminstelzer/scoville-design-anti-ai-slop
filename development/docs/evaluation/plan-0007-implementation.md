# Practical guidance implementation evidence

Date: 2026-09-05. PLAN-0007 is authorized for local implementation. Installation, publication and historical evaluation changes remain outside scope.

**Post-implementation review, 2026-09-05:** [Independent Astra/Fable implementation review](../reviews/0024-practical-guidance-implementation-review.md) supersedes this report's blanket claim that all required local evidence gaps are closed. Both accept the practical runtime text; the required coupled measure/leading comparison is still unverified, the PG-11 Waiting subtitle is inconsistent, and Cartography evidence covers point labels only. The review records the remaining actions, 17 advisory common-load ceiling exceedances, and PG-06's reported reasoning-token asymmetry. Original results and the reviewed report are preserved; neither a narrowed acceptance criterion nor additional successful trials are implied by this notice.

## Freeze and evidence limits

The unchanged runtime baseline is `Z:/Projekts/AI/output/design-plan7-baseline-runtime`, manifest SHA-256 `5474ADF946D3742ED6B364027A4033A80B1660C9603F815BFC3D1F8A2199A5FF`; its build receipt is `docs/evaluation/plan-0007-baseline-build.json`. The full authoring source is separately preserved under `output/design-plan7-trials/source-baseline`. All 100 originally monitored runtime/planning files were unchanged before the intentional lifecycle transition. PLAN-0006's W-004 text is preserved.

Protocol `evaluation/plan-0007/protocol.json` and `run_cases.py` froze 16 case families plus fresh transfer before runtime edits. The external freeze SHA-256 is `F4B43EC8781C8688ECDC48DEFEF74B4379F7E459136A3C06AEC5F764AE2A38A3`; `evaluation/plan-0007/freeze-receipt.json` carries the exact source/runtime manifests. Inputs include all subquestions, original fictional content, controls, playback contracts and geometry. The P2 late-comparison rule preserves the first candidate-only outcome. Packaged teaching fragments use different examples.

Executors request the user's current configured `gpt-6-astra` / `high`, with fresh ephemeral read-only CLI sessions, user customization/plugins/memories/other Skills disabled, and selected Core/owner text supplied inline. Actual transcript, source exposure hashes, prompt, answer, usage and failure records are retained per case/arm under `output/design-plan7-trials`. This is explicit owner exposure, not automatic host activation or authenticated file-read qualification. The optional partner lane is not activated or simulated.

The renderer is bundled Playwright with Chrome `152.0.7977.76`, local Windows Arial/Georgia and blocked HTTP(S). Font hashes are in the freeze. Chrome's version was probed before the first runtime edit and is recorded in each render receipt; it was inadvertently not copied into freeze.json, despite that file's descriptive renderer wording. Original freeze bytes remain intact. Images, DOM/actual-font observations and motion receipts are in `output/playwright/design-plan7`, indexed by neutral V identifiers. The coordinator inspects artifacts before explanations; this is not independent or fully arm-blind adjudication. No audience, native-script, physical, legal or provider acceptance is inferred.

## W-001: foundations

Added concrete grid/weight/grouping operations to Composition, a real-text hierarchy/setting comparison to Typography, a compact technical substitution specimen to Font Technology, and role-palette construction/alternate-theme guidance to Colour. Existing local-synthesis provenance covers these original procedures; no Butterick ranges, foreign brand values or new universal preset is imported.

Core is **not added because already sufficient**: Studio loop steps 3–5 already require the editable artifact, target inspection, smallest repair and controlled comparisons. PG-01 exposes no missing Core working-probe behavior. Core bytes and routing remain unchanged.

| Case | Observed source/render evidence | Disposition and limit |
| --- | --- | --- |
| PG-01 | Both arms move Studio 4 into the date/time group, preserve preparation and booking anchors, and retain the symmetric memorial. Baseline main V52919824b5; candidate main V7634e2ce76. | Application and legitimate countercase observed. Baseline already succeeds; no improvement claim. Added content-derived construction procedure remains explicit local guidance. |
| PG-02 | Both arms set the real text in Georgia and an actually rendered Arial substitution; Chrome CSS font inspection confirms both families. Candidate Vfeee79921a, baseline Ve26ab3217a. Shared heading/body hierarchy and text setting remain inspectable. Single-family utility controls remain single-family. | Application and technical substitution observed. Candidate repeats the paragraph once in each of two settings; baseline repeats it twice per setting. Preserve that output difference; this is no controlled reading-performance comparison. Native-script quality and font licensing are not evaluated. |
| PG-03 | Both arms preserve status labels and action attention in light/dark and monochrome layouts. Candidate V19e9da8291 and V48d47f31bd use an area-bounded accent, role remapping and redundant status cues. | Application/countercase observed; baseline also succeeds. No interaction, general accessibility or colour-vision population claim. |

All six executor sessions exited 0. The package validator and generated-index check pass; the former reports 20 advisory size notices, including pre-existing ones. No token ceiling was promoted into an acceptance gate. Basic body settings and palette choices are visibly usable here, not evidence of universal readability or superior model performance.

## W-002: imagery, brand and mark

PG-04 baseline V1c11704afa / V26ab9aa162 and candidate V9dc71a4dc4 / V514adc9094 were inspected in full. Both preserve two plates and one pin, positive/reversed 24px and 96px views, three distinct labeled icons and an actual 480x160 repeated surface. Candidate `#plate-left` replaces the supplied bowed/asymmetric cubic contour with straight segments and tangent corner curves; the supplied before-state remains visible. Tile guides at x120/240/360 expose seam/density decisions; the control intentionally varies placement and crops edges. Optical suitability here is a coordinator judgment, not identity recognition or trademark clearance. Baseline also succeeds.

PG-05 baseline Vb767f77854 / V4adf503900 and candidate V6903e2bd6a / Vb22ad16408 preserve the lamp/light/book relation. Candidate wide `viewBox="0 0 900 600"`, square `viewBox="150 0 600 600"` and value study all reuse `#scene`; they are actual crops/variants of one constructed source. The flat geometric control remains valid. These artifacts demonstrate illustration/framing branches only; no photography, acquisition or documentary truth is claimed. Retain the three owner-local additions as explicit construction assistance; no causal performance claim.

## W-003: information, diagram and map

PG-06 baseline V5ff31e6161 / Vb2ea849145 and candidate V7e9d8b5b2a / Vf1036230db preserve North=42, South=18, East=31, West=27 items, a common zero-based bar scale and exact lookup. Ordered load levels 1–5 retain 4,7,6,9,11 ms and a legitimate connected line in both controls. **The original candidate control fails visual quality:** its L-shaped SVG axis inherits the default black fill and paints an unintended triangle. Correct values and exit code 0 do not make that render pass.

The coordinator's separate `output/playwright/design-plan7/followup-v2/PG-06-control-host-repair.html` adds only `fill:none` to `.baseline`. Original SHA-256 `1bb7f89d668a538c548b286f505b0c0aa446356b7e86e41865ae9f7479f38786` remains preserved; repair SHA-256 `458b736e3f2125c8db42abac73e0f4ceb7e79a3108ed05467f123c3b64312747`. The separately inspected PNG shows a clean axis, intact line/points, labels and lookup. This closes the local artifact defect through source repair and re-render; it **does not convert the original autonomous result into a pass**. The question/form method is retained with this negative execution evidence and mandatory existing render/repair boundary. No SVG-specific rule is added to Design: an inherited implementation fill is not evidence that chart choice needs another universal prescription.

PG-07 baseline Va5a0a84bf1 / Vb7f7ffa4e2 and candidate V4679d80c5a / V2d42a3fd9b contain exactly five nodes and the five supplied directed/labeled relations. `Repair → Inspect / recheck` uses a distinct return corridor and one Inspect node. The three peer relations stay undirected and symmetric. PG-08 candidate V219341e469 / V5e226d1c16 keeps the supplied x/y coordinates and extent at 600px and 300px; only labels/leaders move. The ranked control preserves Workshop9, Library4, Depot2, Gate1 without an unnecessary map. This is fictional coordinate/geometry proof, not GIS, comprehension or accessible-route qualification. All three owner additions retained; PG-06 retains the failure/repair limit above.

## W-004: workflow and medium

PG-09 baseline Ve504e7d60e / V4680ef40cc and candidate V56d2c96b4b / Ve37e966872 were inspected at 360,720,1200px. Both keep all three exact repair IDs, names, statuses and actions plus the availability note. Candidate changes the wide columns into a name/status/action arrangement at intermediate width and stacked rows at narrow width, while keeping actions associated with their rows. The single-column contact control is unchanged in purpose/content. Browser interaction in `followup-v2/receipt.json` opens R-004, edits its name locally, applies the preview and reloads to the original in both arms. This verifies that narrow prototype path only, not persistence, backend or complete accessibility mechanics.

PG-10 baseline V799264429d / V04dc3a722f and candidate Vb84f681e0d / V138e49e419 were inspected through all three live frames, both reading pages and the one-page reference. Each full medium preserves 24 received = 18 repaired + 4 awaiting parts + 2 returned unchanged, the hinge-before-shade lesson and 22 November. The candidate allocates intake/outcomes/lesson+date to live frames and results/lesson+date to reading pages; the baseline uses a different valid rhythm. Dense reference controls remain readable. This is editable HTML medium allocation and browser appearance, not PPTX/DOCX, presenter testing or printer acceptance. Three additions retained with these scope limits.

## W-005: temporal choices

PG-11 baseline V83fff1d41d / Ve36ed5f40f and candidate V27a9404f0c / Vc1bd43e09d supply actual replayable motion and static controls. Story playback captures show changing hinge poses and Folded/Unfolding/Workshop open; the candidate separates a 360ms fold, 650ms reading hold and 1150ms unfolding, while the baseline uses another authored sequence. These are fixture choices, not imported timing rules. Reduced versions retain final meaning and static descriptions without running animations.

The first full-page capture did not expose the recurring Waiting state: the pages settle motion when the observed study leaves the viewport. A supplementary 1200x1200 viewport keeps both studies visible, without editing either artifact. `followup-v2/receipt.json` observes Waiting at +100ms, Ready with a running confirmation effect after a further 850ms, and Ready at rest after 400ms, in both arms. A second click interrupts to Ready; reduced status ends Ready with no animation. Screenshots were inspected alongside sampled runtime state and recorded playback. This corrects a capture gap, not an unchanged provider rerun. No flashing was observed; no general motion-safety or timing-optimality claim. Motion addition retained.

## W-006: communication and transfer

PG-12 candidate Ve1d8bdd807 / V27745c2a24 preserves the supplied event facts, explicitly records unknown venue/time/year/organizer/cost, and depicts two different carriers: a visibly mended word and an unlit/lit lamp comparison. The near-variant changes stitch colour only; the quiet literal notice remains legitimate. Counter-readings include textile repair versus lamp repair and a switch-on/guaranteed-result reading. This is visible carrier discrimination, not audience comprehension or campaign-effectiveness evidence.

PG-13 candidate V452deead5d / V290628c0ce preserves circle→A, square→B, triangle→mat. Its circle example, incorrect square-in-A with correction, and rotated triangle practice instance are distinct. Browser capture `followup-v2/PG-13-revealed.png` shows the previously hidden answer after a real button click: triangle stays on the mat. Plain text remains a valid control. No learning-transfer claim about people follows from a functioning new-instance exercise.

**Coverage correction:** PG-12's conditional wording did not actually request both media, so it did not exercise row28's style translation. The original result and freeze remain unchanged. `evaluation/plan-0007/style-revision-freeze.json` freezes a separate PG-12S task before its executor call: a supplied fictional interrupted-word/bridge relation, same event facts, poster and compact notice plus literal control. This is a new P2 coverage task, not a repeat seeking a better PG-12 result. Its inspected V11d693114a / Vc5e3dd0c59 artifacts preserve the bridge between work/ing in two separately composed media. The 760x1000 poster puts event/date at opposite upper edges and the instruction below; the 360px notice uses a shorter bridge and gathers facts beneath the headline. The exact proposition and facts survive; the literal control removes the bridge while retaining the two allocations. The collapsed wording editor is incidental, not a new required workflow. All five additions are retained with local P2 application/countercase evidence; no historical or audience claim.

## W-007: package and SKU

PG-14 candidate Vfe1ca8e50d / Vf54ce51ab2 shows Heavy and Light on identical supplied panels and in colour/grayscale front+right-side previews. Source symbols and `<use>` elements reconcile BACK x0/w100, SIDE-L x100/w40, FRONT x140/w100, SIDE-R x240/w40, all h160. Front and side symbols are reused in assembly rather than separately redrawn. Hinge Care, Heavy/Light, 100 g, side SKU-H/SKU-L and the back warning are retained. Small assembled fronts keep variant words/initials distinguishable; equal-layout control is valid. Occluded back/left and absent top/bottom details are explicitly outside the preview. Packaging addition retained; no physical construction, print, barcode or regulatory qualification.

## W-008: wayfinding

PG-15 candidate V9389e55df2 / Vf4fd37a0dd preserves the entire supplied route and registry. Entrance-facing Junction arrows point left to Workshop and right to Gallery via Stairs, then up. Arrival says Gallery without a misleading directional arrow. From Workshop, recovery travels ahead through Junction toward Stairs; Entrance is right. Returning from Stairs, Entrance is left. The straight-corridor control has only arrival identification and no invented branch sign. Wayfinding addition retained on schematic route/approach evidence, with no distances, installation, real accessible-route or safety acceptance inferred.

## W-009: production, critique and unchanged owners

PG-16 returns no files, correctly preserving read-only scope. The answer locates unsupported `main > section > strong` ZERO IMPACT against the supplied `small` sleeve-only 40% recycled-fibre claim. It proposes removing the unsupported claim and restoring prominence to the exact qualified claim while preserving the required Sample Studio credit and fictional-event disclosure. It distinguishes local HTML export from printer acceptance and preserves the quiet centered title as a no-defect owner-local control. This is source-located critique evidence; no nonexistent export or render is claimed.

Production and Critique additions retained. All seven originally retained owners remain byte-identical to the frozen source; targeted PG-16 boundaries concern claim/source fidelity, supplied credit, fictional mode and solo no-partner behavior only. Cultural/community authority, real rights clearance and an actual partner exchange were not exercised or requalified. Core, modules.yaml and agent metadata are also unchanged. Of 100 originally monitored files, 74 remain identical; changed monitored files are the 23 intended leaves, source-index, PROJECT_INDEX and PLAN-0006's authorized lifecycle header/note. Existing Decisions and earlier work evidence are preserved.

## Capture failure accounting

The first supplementary capture attempted `input[name="object"]` although the artifact uses a textarea. It timed out before the interaction result; the partial chart repair remains under `output/playwright/design-plan7/followup`. The corrected helper selects `[name="object"]` and writes fresh `followup-v2` receipts/artifacts. This was a coordinator harness error, with no change or retry of a provider answer.

## W-010: combined candidate and delivery

Fresh PG-T1 candidate Vf4b25b6164 / V85ca001051 was inspected at 360,720,1000px. Collection day/time/room stay grouped, return/card instructions remain separate, and 8 available / 3 reserved stays an explicit distinction. All required strings survive the colour and quiet monochrome variants without horizontal overflow. The small telescope illustration is subject-specific and makes no identity or new service claim. This supports bounded composition/type/colour/responsive transfer to the held-out brief; it does not establish general model superiority or automatic Skill routing.

`plan-0007-trial-receipts.json` collects 28 unique executor sessions: ten baseline/candidate pairs, six original candidate-only cases, the explicit PG-12S coverage correction, and one fresh transfer. It binds prompts, exposed source hashes, answers, artifact hashes, usage and 54 original rendered HTML artifacts to local receipts. All executions exited 0, but **these are not 28 autonomous design passes**: PG-06's original render failure and coordinator repair remain separate. The supplementary repair and interaction images are additional derived evidence, not another provider result. All final candidate exposure hashes match the delivered owner files, and neither freeze changed. Raw transcripts, videos and HTML remain local under the recorded output paths; the repository contains the compact evidence records, not a claim of public artifact availability.

The built local runtime is `Z:/Projekts/AI/output/design-plan7-final-runtime`, 35 files. Source and runtime both have manifest SHA-256 `1F2CD88F80964FC6ED71C6B4A346A93322B9818FE3B9471DFF466074AF7840CE`; `plan-0007-final-build.json` records the existing transform. A fresh manifest computation after the transfer matches both. Core/index, routing signals, ownership, module count and dependencies are unchanged. `modules.yaml` keeps its historical receipt IDs and draft statuses; the new scoped records above do not promote those earlier outcomes into final qualification. No installation or publication was performed.

Final checks in `plan-0007-checks.json`: package structural validation passed; generated index current; 60 routing cases / 44 signals / 30 modules valid; Design/UI boundary valid; all 34 existing tests passed; Skill Creator quick validation passed. The package validator emits **42 advisory size notices**, including existing ones. They remain visible; no threshold was raised to hide the cost. After closing PLAN-0007 and setting `active_plan: null`, native Plan validation passed with zero errors/warnings across 66 profile files, seven Plans, 47 Work Items and 58 Decisions. `git diff --check` passed. PLAN-0006 remains draft with its paused W-004 unchanged.

The comparable `o200k_base` measurements in `plan-0007-baseline-tokens.json`, `plan-0007-final-tokens.json` and the consolidated receipt show Core+index unchanged at 3,072. Across all leaves plus Core, instruction text grows from 57,729 to 62,317 (+4,588; about 7.95%). Each changed leaf adds 132–280 tokens; affected declared common loads add 145–952. The complete before/after table for every leaf and common load is retained in the consolidated receipt. This is file loading cost, not a measurement of provider-total cost or a requirement to load the whole package. The additional cost buys explicit operations and worked fragments; practical benefit remains bounded to the observations above.

## Final module dispositions

Rows refer to the immutable audit/contract mapping; evidence tier and source scope remain as defined there. `Retained with evidence` means the added operational mechanism has the local application/control evidence and limits described above, not that every example branch or professional capability was independently qualified. The seven no-extension judgments preserve existing rules and historical evidence.

| Row | Owner | Final disposition | Evidence |
| --- | --- | --- | --- |
| Core | Studio loop and routing | Not added because already sufficient | W-001, PG-01; unchanged bytes |
| 01 | Brief | Retained with evidence | W-006, PG-12 |
| 02 | Concept | Retained with evidence | W-006, PG-12 |
| 03 | Composition | Retained with evidence | W-001, PG-01; PG-T1 |
| 04 | Typography | Retained with evidence | W-001, PG-02; PG-T1 |
| 05 | Font Technology | Retained with evidence | W-001, PG-02 actual fonts |
| 06 | Colour | Retained with evidence | W-001, PG-03; PG-T1 |
| 07 | Imagery | Retained with evidence | W-002, PG-05 constructed illustration branch |
| 08 | Information | Retained with evidence | W-003, PG-06 original render failure plus separate local repair; no autonomous pass |
| 09 | Cartography | Retained with evidence | W-003, PG-08 |
| 10 | Diagrams | Retained with evidence | W-003, PG-07 |
| 11 | Brand | Retained with evidence | W-002, PG-04 |
| 12 | Mark | Retained with evidence | W-002, PG-04 |
| 13 | Instructional | Retained with evidence | W-006, PG-13 interaction |
| 14 | Advertising | Retained with evidence | W-006, PG-12 |
| 15 | UI Workflow | Retained with evidence | W-004, PG-09 prototype interaction |
| 16 | Web | Retained with evidence | W-004, PG-09; PG-T1 |
| 17 | Fixed Media | Retained with evidence | W-004, PG-10 HTML sequences |
| 18 | Packaging | Retained with evidence | W-007, PG-14 supplied geometry |
| 19 | Wayfinding | Retained with evidence | W-008, PG-15 schematic route |
| 20 | Motion | Retained with evidence | W-005, PG-11 actual playback and corrected capture |
| 21 | Production | Retained with evidence | W-009, PG-16 critique boundary; W-010 real source/runtime receipt |
| 22 | Critique | Retained with evidence | W-009, PG-16 source-located read-only finding |
| 23 | Culture | Not added because already sufficient | Original audit retain judgment; unchanged, no community requalification |
| 24 | People / Media Integrity | Not added because already sufficient | Original audit; W-009 constructed-mode boundary only |
| 25 | Sustainability | Not added because already sufficient | Original audit; W-009 supplied narrow claim only |
| 26 | Source Evidence | Not added because already sufficient | Original audit; W-009 exact claim/source boundary |
| 27 | Asset Rights | Not added because already sufficient | Original audit; W-009 supplied credit, no new rights clearance |
| 28 | Style | Retained with evidence | W-006, explicit PG-12S replaces the missing conditional lane only |
| 29 | Generic Signatures | Not added because already sufficient | Original audit; quiet controls, no prevalence claim |
| 30 | Sibling Coordination | Not added because already sufficient | Original audit; solo no-partner boundary, no actual partner qualification |

Bookkeeping: 23 retained additions and eight no-extension dispositions, including Core. There is no unresolved local implementation or required capture gap in PLAN-0007. General efficacy, audience/native-script/physical/provider/actual-partner qualification remain outside this evidence; PLAN-0006 W-004's existing unfinished work is preserved separately. Astra and Fable reviewed the Plan, not this final runtime tree; their exact outcomes and resolved findings remain in `docs/reviews/0023-practical-guidance-plan-review.md`.
