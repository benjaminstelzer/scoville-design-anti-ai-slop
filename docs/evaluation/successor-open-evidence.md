# Successor open Terra High evidence

Date: 2026-09-02  
Status: W-012 in progress  
Model: exact `gpt-5.6-terra`  
Reasoning: `high`

This ledger records terminal open calls against the professional-depth
successor. It is not sealed holdout, cross-person preference, general
professional-competence, or market evidence. Local licensed fonts, generated
artifacts, raw traces, and screenshots remain outside the public repository.

## Model-free package gate

- successor schema: 23 exact flat modules, valid;
- Core: 1,273 `o200k_base` tokens;
- generated index: 934 tokens;
- largest expert: 2,174 tokens;
- Core plus index plus four largest experts: 10,220 tokens;
- validator tests: 17/17;
- route fixtures: 19/19 across 34 signals;
- leaf contract: 23 leaves, 22 cases, six dimensions, five canaries;
- Skill Creator: Design and UI valid;
- local Markdown links and generated index: valid;
- current package manifest after the C06 Core routing repair:
  `853F59B6B4BFA91D42DC6C8641E2A444E9723C59C89B9F11386D91BEC20F6EA3`.

Five experts exceed the 1,800-token warning target: Typography 2,014, Font
Technology 1,943, Motion 1,825, Production 2,174, and Style 1,882. Each remains
below its measured ceiling. Coverage was preserved rather than shortened to
silence the warning.

## C01 call history

| Run | Provider total | Cached input | Uncached input + output | Result |
| --- | ---: | ---: | ---: | --- |
| SkillOpt R1 | 498,879 | 451,584 | 47,295 | Invalid harness/case: SkillOpt target is read-only, asset paths were ambiguous, and the prompt itself opened Font Technology, Fixed Media, Production, and Asset Rights while Gold forbade them. No artifact. |
| C01-R2 | 144,882 | 115,712 | 29,170 | Exact Composition + Typography route and good unchanged HTML render; failed because it added an explicitly unrequested responsive media query. |
| C01-R3 | 187,495 | 149,248 | 38,247 | Exact route and valid unchanged HTML render; the strengthened Core still could not remove the browser-format ambiguity and another unrequested media query appeared. |
| C01-R4 | 156,338 | 123,904 | 32,434 | **Pass.** The corrected contract used a fixed SVG and selected Composition, Typography, and Fixed Media exactly. |
| **Total** | **987,594** | **840,448** | **147,146** | Four terminal calls; R1-R3 consume three of four coverage reserves. |

R1 was not retried unchanged. R2 and R3 exposed that a browser artifact was a
bad isolation mechanism, not that the Web leaf should be hidden. The accepted
registry says a fixed digital canvas belongs to Fixed Media, so C01 and its
machine-readable matrix were corrected to select that owner rather than
forcing an artificial two-leaf Gold.

## C01-R4 accepted evidence

Selected experts, in Core index order:

1. `composition-and-layout`
2. `typography-and-typesetting`
3. `editorial-and-fixed-media-design`

Hard checks passed:

- Core, brief/font receipt, and the three experts were each read once in three
  batched read calls;
- no sibling, source-index, Font Technology, Production, Rights, Web, or
  Critique read occurred;
- exact required text, 1,200 × 1,600 dimensions, viewBox, live text, local font
  URLs, XML, and no alternate/media/script/raster behavior passed;
- the predeclared display exception stayed with Typography;
- the unchanged artifact rendered offline with only declared local file
  requests; intended render and proportional thumbnail exist;
- artifact SHA-256:
  `FA8E09BF6B9107472ECB123FFDD9AC7EDCB37D8C51E1A005F8E623A8599DAD57`;
- intended render SHA-256:
  `BF39D5286585B61B4419D8D938AE6DFFD7CDC89B11FDA3937633BF8951D6F132`;
- thumbnail SHA-256:
  `5033EA7CFA134B4BDD1BCDE7EBFF3141E7A40F5A172797A772DB0EF0A5342684`.

One-reviewer visual inspection found a strong subject-specific fixed
composition, clear entry and event sequence, a recognizable compressed title
at both views, coherent two-family contrast, useful negative space, and a
visible access/action finish. Two minor observations remain: the compact final
type record should explicitly name the serif narrative lead as a display/deck
role, and the duplicated venue must remain an intentional fixed-medium anchor.
Neither caused content, hierarchy, fit, exception, or route failure in C01.

Two source-validation shell attempts failed before a third local check passed;
the terminal artifact and model response remained valid. This is efficiency
evidence, not a second provider request.

## Budget state after C01

- provider calls used: 4 of 46;
- provider-total used: 987,594 of 5,000,000;
- uncached-input-plus-output used: 147,146 of 2,750,000;
- coverage reserve remaining: 1 call;
- sealed holdout: unopened and unauthorized by this ledger.

No further call may run without inspecting the preceding result. Any new
infrastructure or Gold defect stops the sequence; the remaining reserve cannot
be spent on another ambiguous format experiment.

## C03 Font Technology safety floor

C03-R1 passed in one terminal call and loaded only
`font-technology-and-script-safety`. It repaired a supplied mixed English,
Arabic, and Japanese notice without changing its locked content or layout:

- Arabic gained the supplied Noto Sans Arabic font, `lang="ar"`, a local RTL
  boundary, normal synthesis/style/spacing, and preserved logical source order;
- the mixed Latin gate identifier and Arabic platform phrase use explicit Bidi
  isolation rather than character reversal;
- Japanese gained the supplied Noto Sans JP font, `lang="ja"`,
  `writing-mode: vertical-rl`, and upright orientation instead of a rotated
  horizontal line;
- all strings, English root direction, and locked regions survived;
- HTML/source checks and an unchanged offline render passed;
- native-reader approval, full script correctness, access, licence clearance,
  and browser universality remained explicitly unclaimed.

Provider usage was 170,217 total tokens: 163,934 input, 123,648 cached input,
40,286 uncached input, and 6,283 output. Artifact SHA-256:
`24929BD25E9874F945F32189BB7409A702841D66B3BCF575D9B863390DA5E473`.
Intended render SHA-256:
`4B82E5A8008D6884EDFE1FCE62E1154625B6D833359180FF244EC8EF97F8BDB2`.
One-reviewer inspection was limited to visible technical behavior; Arabic and
Japanese native correctness remains unverified.

## Budget state after Wave A

- provider calls used: 5 of 46;
- completed coverage identities: C01 and C03;
- provider-total used: 1,157,811 of 5,000,000;
- uncached-input-plus-output used: 193,715 of 2,750,000;
- coverage reserve remaining: 1 call;
- next canary: C06; no sealed job is authorized.

The four successful workspace-write calls averaged 164,733 provider-total
tokens, materially above the original 70,000 planning estimate. If that rate
continues, twenty remaining coverage calls would use about 3.29 million and
leave roughly 0.55 million under the hard provider-total ceiling for SkillOpt.
This is a risk forecast, not authorization to exceed the ceiling. Later cases
must minimize redundant file and validation output, and W-012 stops before the
hard ceiling rather than forcing all planned optimization pairs.

## C06 Brief and Concept routing repair history

C06-R1 produced a strong source-bounded frame, exact content manifest, hard
gates, authority map, three same-content roughs with different primary
carriers, a conditional utility selection, explicit reversibility evidence,
and no invented translation, research, route, access, strategy, feasibility,
or approval. It nevertheless loaded `critique-and-validation` in addition to
Brief Framing and Concept Development. That third read is a hard modularity and
efficiency failure: ordinary concept candidate comparison, selection, owner
validation, and a local utility exception belong to Concept.

The first correction narrowed the Critique index label and added an explicit
negative boundary inside the leaf. C06-R2 produced another good frame and
concept record but still opened Critique before it could read that internal
negative. The executable Core therefore now states the negative routing rule
before any leaf read: owner-local candidate comparison, output validation,
claim boundaries, and declared local exceptions do not select Critique; only a
separate multi-domain finding, controlled before/control/after, challenged
cross-domain exception, or generic-cliche lifecycle does.

| Run | Provider total | Cached input | Uncached input + output | Result |
| --- | ---: | ---: | ---: | --- |
| C06-R1 | 91,007 | 63,232 | 27,775 | Strong content; extra Critique read. |
| C06-R2 | 134,836 | 85,504 | 49,332 | Strong content; extra Critique read after index-only correction. |
| C06-R3 | 104,749 | 74,240 | 30,509 | **Pass.** Exact Brief Framing + Concept Development route and accepted source-bounded concept record. |

C06-R3 read Core once, the brief once, and exactly
`brief-framing-and-criteria` plus `concept-development-and-selection`; it did
not read Critique, the source index, or another expert. The result contains
three equal-fidelity monochrome mechanisms with materially different decision
orders, including the required one-decision-at-a-time quiet-utility exception.
Every rough retains all five exact strings and exposes the missing approved
translation. Gates precede comparison; the selected direction is conditional,
names evidence and owners that could reverse it, and remains blocked from
release. No typography, colour, imagery, map, responsive, UI, or production
execution was added.

All deterministic checks and one-reviewer source-bounded inspection passed.
Artifact SHA-256:
`7244C8AFB69434F2BDDFAD146FF3EDF703C8A299B7F5DE62CD5379E0903F77C0`.
Raw-event SHA-256:
`4DD57D777516FE6BD80F691332B76A4E4CA982D7CC0B9580C16A162F4653D945`.
The evidence proves an explicit, traceable frame and bounded concept
selection; it does not prove audience behavior, translation, accessibility,
route facts, production, originality, or release approval.

## Budget state after accepted C06-R3

- provider calls used: 8 of 46;
- planned coverage identities completed: 3 of 22;
- provider-total used: 1,488,403 of 5,000,000;
- uncached-input-plus-output used: 301,331 of 2,750,000;
- coverage reserve remaining: 0;
- completed coverage identities: C01, C03, and C06;
- C06: exact route and artifact contract passed;
- next planned coverage identity: C02, subject to its frozen local preflight and
  the same immediate inspection rule.

One planned SkillOpt call can be reassigned to the C06 coverage retest while
keeping the global 46-call and both token ceilings unchanged, but that changes
the frozen lane allocation and requires user authorization. Alternatively the
coverage reserve can be increased, which also requires explicit authorization.

The user authorized continuation on 2026-09-02. One planned SkillOpt call was
therefore reassigned: coverage reserve is five, planned SkillOpt calls are
fifteen, SkillOpt reserve remains four, and the global 46-call plus token
ceilings remain unchanged. This authorizes one C06-R3 retest only; it does not
hide or rewrite R1/R2. C06-R3 consumed that final coverage reserve and passed;
no further coverage retry is available without another explicit lane-budget
decision.

## C02-R1 read-only critique stop

C02-R1 selected exactly `typography-and-typesetting`,
`web-and-responsive-design`, and `style-direction`. It read no Composition,
Critique, source-index, or other expert and changed no file. Its three findings
were substantively useful and correctly isolated:

- weak adjacent metadata/title roles, justified narrow serif measures with
  visible rivers, and untreated punctuation to Typography;
- the fixed 980-pixel program, 820-pixel note, clipped 310-pixel intro, and
  masked horizontal overflow at 390 pixels to responsive Web;
- unsupported repeated wave glyphs, gradient, and glow to Style rather than to
  the accepted wide-screen composition.

The response preserved the approved cropped `CURRENT` display exception,
proposed parent-level repairs, kept the critique read-only, and stated the
audience/access/runtime/style-authenticity evidence ceiling. It nevertheless
reported `render_inspected: true` without any image-view event or attached
image input in the raw trace. The screenshots were merely present as workspace
files. C02-R1 therefore **fails evidence honesty** even though its source-based
diagnosis is useful; it does not qualify C02.

Provider usage was 105,748 total tokens: 103,043 input, 70,400 cached input,
32,643 uncached input, and 2,705 output. Raw-event SHA-256:
`226621A7F8ABF43F3799D10B0FDDC1B9E234327B8B5E0A440B42A5DA4D100B8C`.

The local wrapper now supports immutable initial `--image` inputs constrained
to the case workspace and records their hashes. A fresh C02-R2 preflight passes
without a provider call and binds the wide render
`2E30B7FD4F1A34CFBE5FC28B735D8D1799D72B1967876FFDD3AB24EDBE420CCB`
and narrow render
`C83C9F8AF5AB56348AAF4AC0835E4889EDC5BEC5CA94FBA6FE92C449F1121A0F`.
This repairs the input channel but does not retroactively repair C02-R1.

## Budget state at the C02 stop

- provider calls used: 9 of 46;
- provider-total used: 1,594,151 of 5,000,000;
- uncached-input-plus-output used: 336,679 of 2,750,000;
- completed planned coverage identities: C01, C03, and C06;
- C02: useful exact-route source critique, failed render-evidence honesty;
- coverage reserve remaining: 0;
- sealed holdout: unopened;
- next provider call: stopped pending an explicit lane-budget decision.

A C02-R2 retest now has a locally verified attachment path, but no coverage
reserve remains. Running it requires explicit authorization to reassign one
planned or reserve SkillOpt call while keeping the 46-call and token ceilings
unchanged, or a separately authorized increase. No later coverage case may run
around this failed case.
