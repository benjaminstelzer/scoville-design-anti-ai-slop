# RC7 closed-vocabulary Design routing regression v9

Created: 2026-09-02 after repeated v3 through v8 routing instability.

V8 remains unchanged. V9 keeps the same twelve open cases and tests the Core
rule that explicit material index concerns must load while generic words such
as `design`, `audit`, and `review` cannot invent a domain. It is executed three
times per split to measure routing stability rather than one favorable draw.

V9 is an open regression suite, not fresh unseen evidence. Its copied Test
split remains consumed and is not run or claimed as unseen.
